#include <stdio.h>


int main(){

    // number of edges
    int n;
    printf("\nNumber of edges : ");
    scanf("%d",&n);

    int tc = n;
    
    while(tc--){
        int a , b;
        scanf("%d -> %d",&a , &b);
    }

    return 0;
}